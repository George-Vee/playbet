(function(){document.addEventListener('DOMContentLoaded',function(){initHeroCarousel();initVeeSliders()});function initHeroCarousel(){var carousel=document.querySelector('.veeaardvark-carousel');if(!carousel)return;var track=carousel.querySelector('.veeaardvark-slides');var slides=carousel.querySelectorAll('.veeaardvark-slide');var prevBtn=carousel.querySelector('[data-car-prev]');var nextBtn=carousel.querySelector('[data-car-next]');var dotsContainer=document.getElementById('veeaardvark-bannerDots');if(!track||!slides.length)return;var index=0;var autoTimer=null;var AUTO_MS=6000;function goToSlide(i){index=(i+slides.length)%slides.length;track.style.transform='translateX('+(-index*100)+'%)';updateDots()}
function updateDots(){if(!dotsContainer)return;var dots=dotsContainer.querySelectorAll('.veeaardvark-dot');dots.forEach(function(dot,idx){var isCurrent=idx===index;dot.setAttribute('aria-current',isCurrent?'true':'false');if(isCurrent){dot.classList.add('is-active')}else{dot.classList.remove('is-active')}})}
function startAuto(){stopAuto();autoTimer=setInterval(function(){goToSlide(index+1)},AUTO_MS)}
function stopAuto(){if(autoTimer){clearInterval(autoTimer);autoTimer=null}}
if(prevBtn){prevBtn.addEventListener('click',function(){goToSlide(index-1)})}
if(nextBtn){nextBtn.addEventListener('click',function(){goToSlide(index+1)})}
if(dotsContainer){var dots=dotsContainer.querySelectorAll('.veeaardvark-dot');dots.forEach(function(dot,idx){dot.addEventListener('click',function(){goToSlide(idx)})})}
carousel.addEventListener('mouseenter',stopAuto);carousel.addEventListener('mouseleave',startAuto);goToSlide(0);startAuto()}
function initVeeSliders(){var sliders=document.querySelectorAll('.vee-slider');if(!sliders.length)return;sliders.forEach(function(slider){var viewport=slider.querySelector('.vee-slider-viewport');var track=slider.querySelector('.vee-slider-track');var prevBtn=slider.querySelector('[data-slider-prev]');var nextBtn=slider.querySelector('[data-slider-next]');if(!viewport||!track||!prevBtn||!nextBtn)return;function getMaxScroll(){return Math.max(0,viewport.scrollWidth-viewport.clientWidth)}
function updateButtons(){var maxScroll=getMaxScroll();var left=viewport.scrollLeft;prevBtn.disabled=left<=0;nextBtn.disabled=left>=maxScroll-1}
function scrollByPage(direction){var page=viewport.clientWidth*0.9;var target=viewport.scrollLeft+direction*page;var maxScroll=getMaxScroll();if(target<0)target=0;if(target>maxScroll)target=maxScroll;viewport.scrollTo({left:target,behavior:'smooth'})}
prevBtn.addEventListener('click',function(){scrollByPage(-1)});nextBtn.addEventListener('click',function(){scrollByPage(1)});viewport.addEventListener('scroll',debounce(updateButtons,50));window.addEventListener('resize',debounce(updateButtons,150));updateButtons()})}
function debounce(fn,wait){var timeout=null;return function(){var ctx=this;var args=arguments;clearTimeout(timeout);timeout=setTimeout(function(){fn.apply(ctx,args)},wait)}}})()
